// Time and date program

#include <stdint.h>	// Integers library
#include <stdio.h>	// Input/Output library

void dayMonthYear(void) {
// A struct of dates
	struct Date {
	uint8 day;
	uint8 month;
	uint16 year;
	};
// Todays date
	struct Date today = {3, 3, 2026}
}

int main(void) {
	while(1) {
	// funkcje na potem
	}
return 0;
}